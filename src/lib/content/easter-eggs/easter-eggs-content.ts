/**
 * Animation input for the intro MatrixTerminal, deliberately kept in TypeScript: these are typed-out
 * terminal lines rather than page prose, and `MatrixTerminal` needs them as a string array. All the
 * actual page content lives in `src/content/easter-egg-hunt/content.mdx`.
 */
export const easterEggHuntIntroLines: string[] = [
    "Wake up...",
    "You've been here before — but have you seen everything?",
    "Some secrets are hidden in this site. Four of them, for now.",
    "Follow the clues. Trigger them yourself.",
];
