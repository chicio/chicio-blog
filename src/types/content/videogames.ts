export type ConsoleMetadata = {
  releaseYear: string;
  acquiredYear: string;
  bits: string;
  generation: string;
  manufacturer: string;
  name: string;
  sku: string;
}

export type GameMetadata = {
  releaseYear: string;
  manufacturer: string;
  console: string;
}
