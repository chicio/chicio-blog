'use client'

import styled from "styled-components";
import {Heading4} from "@/components/design-system/atoms/heading4";

export const ReadNextTitle = styled(Heading4)`
  margin: ${(props) => props.theme.spacing[2]} 0;
`;
